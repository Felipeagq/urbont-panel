/**
 * Versión del panel, a mano.
 *
 * A propósito NO sale de package.json: ese número lo puede tocar cualquier
 * `npm install` o bump de dependencia sin que nadie lo note. Esta constante
 * sólo cambia cuando alguien decide que cambió — subirla es parte del PR
 * que lo amerite, no un efecto secundario.
 */
export const APP_VERSION = 'v1.0.2';
